# 网页实现、预算与开发计划

这是下一阶段的实现建议。当前交付仅含研究、素材和设计；没有游戏构建、性能实测或原作试玩结果。

## 1. 建议路线

**TypeScript + Three.js + Rapier（WASM）+ 简单 DOM 界面**。使用 WebGL2 作为首版渲染基线；把 WebGPU 当后续可选项。采用轻量构建工具组织资产和代码，浏览器端运行，首版无后端服务。

选择依据：Three.js 已有[Rapier 车辆示例](https://threejs.org/examples/physics_rapier_vehicle_controller.html)；[Rapier 车辆接口](https://rapier.rs/javascript3d/classes/DynamicRayCastVehicleController.html)能作为接地、动力和悬挂的起点；[InstancedMesh](https://threejs.org/docs/pages/InstancedMesh.html)适合重复植被合批。它们提供基础能力，**不保证直接获得本策划的越野手感或性能**。

不预先要求 React 或大型游戏引擎导出。若后续已有可复用的成熟驾驶基础，应以实际试验场表现决定复用，避免为换技术重新调一遍车辆。依赖版本在立项时锁定并记录，不在研究阶段宣称某个版本已验证。

## 2. 模块与数据边界

| 模块 | 职责 | 关键输入输出 |
| --- | --- | --- |
| 输入 | 键鼠、手柄、失焦、安全清空 | 统一油门、制动、转向和模式动作 |
| 车辆 | 固定步长动力与悬挂 | 姿态、车轮状态、速度、接地信息 |
| 地形与表面 | 高度场、局部碰撞、材质查询 | 接触位置对应干土／泥／岩石，另查询水深 |
| 绞盘与恢复 | 单绳牵引、有效锚点、安全出生点 | 连接状态、张力限制、复位原因 |
| 相机 | 跟车、环视、防遮挡、摄影 | 独立于车辆物理，不改车辆姿态 |
| 世界渲染 | 森林、地表、水、远山、阴影和雾 | 按块加载与质量档配置 |
| 旅程 | 景点、支路、奖励、保存 | 小型版本化进度数据 |
| 声音 | 车辆状态混音与空间环境声 | 负载、表面、速度、水深、所在区域 |

数值集中存放在车辆／表面／镜头配置中，地标与锚点采用独立数据。物理接触与视觉道路共用空间标记，减少“看起来可走但实际撞墙”。

## 3. 森林制作方式

手工确定道路、坡度、转弯视线、景点构图及救援锚点；树木采用固定种子的分块散布，然后人工清理道路和构图窗口。固定种子只帮助生产，不生成每次不同的核心关卡。

建议 64 m 的空间块，按视野与距离合批渲染。高精度碰撞保留在车附近；大树干、主要岩石和路面真实参与碰撞，小草、落叶与细枝仅显示。块边界的碰撞在车辆离开前不能卸载，相邻高度场无缝接合。

近景树保留轮廓和层次，中景减少几何与阴影，远景用简化模型或树群面片。草的透明叠层和阴影可能比三角形数更贵，需分别测量。雾用于构图与远距简化，不掩盖近处危险。

水优先采用低成本表面着色、岸边泡沫和预设环境反射；没有必要首版实时镜像整个森林。远山不需要高密度碰撞。轮迹与粒子有数量、寿命和空间上限。

## 4. 性能预算（目标，未测量）

先指定目标实机，不把“普通电脑”当验收配置。建议以下两档作为立项样机候选，实际设备、浏览器版本和渲染分辨率写入每次记录。

| 项目 | 标准档目标 | 兼容档目标 |
| --- | --- | --- |
| 候选实机 | Windows、GTX 1650 级独显、16 GB | Intel Iris Xe 级核显、16 GB |
| 画面 | 1920×1080，DPR 上限 1 | 1280×720 或等效动态分辨率 |
| 连续驾驶目标 | 中位帧时间 ≤16.7 ms；p95 ≤22 ms | 中位帧时间 ≤33.3 ms；p95 ≤40 ms |
| 固定物理 | 60 Hz，单步 CPU 起始预算 ≤3 ms | 同样保持 60 Hz，不用降低物理频率伪造性能 |
| 首次可驾驶下载 | 合计目标 ≤12 MB，按压缩传输字节记录 | 相同基础资源，低质贴图可更小 |
| 首版全资源 | 目标 ≤35 MB | 同左；按需载入声音／可选细节 |
| 初始化时间 | 20 Mbps、冷缓存，点击后 ≤10 s 可驾驶 | ≤15 s；明确加载状态 |
| 可见几何起始预算 | 约 ≤300k 三角形 | 约 ≤150k |
| 绘制批次起始预算 | 约 ≤150（含主要阴影开销） | 约 ≤90 |
| 纹理显存估算 | ≤160 MiB | ≤96 MiB |

这些是约束设计的初始预算，不是库或素材包的性能宣传。帧时间按 5 分钟固定路线测 p50/p95，另记超过 100 ms 的卡顿、内存增长和加载停顿；首次载入与热缓存分开记录。浏览器 GPU 内存不总能直接可靠读取，应写明纹理估算法和工具口径。

画质下降顺序：草密度 → 远树细节 → 阴影距离和分辨率 → 水反射 → 渲染分辨率。始终保留道路、障碍碰撞、锚点、危险提示和相同车辆行为。

## 5. 交付阶段与停止扩张条件

估算前提：1 名熟悉浏览器 3D／物理的开发者，另有兼职美术与声音支持；复用已核验许可的基础素材。约 **18–28 人日**，按人员可用性约 4–6 周。是制作量级估计，不是交付保证。

| 阶段 | 预计投入 | 可检查的交付 | 进入下一阶段的条件 |
| --- | --- | --- | --- |
| P0 驾驶灰盒 | 3–5 人日 | 约 100 m 试验路、1 辆占位车、跟车相机、表面切换、复位 | D01–D07 基本通过，原型已能体现低速趣味 |
| P1 森林片段 | 5–7 人日 | 约 300 m 路段、一个景点、溪流、局部泥坑和绞盘、声音 | D08–D12 通过；密林性能符合候选设备预算 |
| P2 完整旅程 | 5–8 人日 | 1.4 km 环线、三景点、两分岔、地图、存档、摄影、轻奖励 | 原厂车全程可走；无流程软锁；可首次通关 |
| P3 打磨验收 | 5–8 人日 | 键鼠和手柄调整、镜头清理、资产减重、体验录像与反馈 | 达到下列整体验收，不依靠删掉核心地形过关 |

P0 不通过就继续调车，不进入大地图制作。P1 低画质仍不达标就降低森林成本或缩小可视范围，不先做第二地图。P2 只允许完善已经定义的环线，不新增联机或车辆库。

## 6. 主要风险与对应处理

| 风险 | 最早验证点 | 处理 |
| --- | --- | --- |
| 射线车在石阶边缘卡顿 | P0 单轮凸起和交错石阶 | 修正碰撞形状、车轮尺寸；仍不成立再评估自写接触 |
| 车辆像悬浮玩具 | P0 静态下沉、松油、制动 | 统一尺度、质量、接触力与阻尼，拒绝渲染贴地补丁 |
| 键盘满油让慢爬很难 | P0 键鼠试玩 | 输入渐变与细油门模式；不以只支持手柄结束 |
| 画面很好但树林卡顿 | P1 密林转弯 | 分别测草、阴影和透明重叠；分块合批 |
| 绞盘穿墙、选错点或拉飞 | P1 固定救援坑 | 少量手工锚点、遮挡检测、张力上限、随时取消 |
| 场景很大但没有体验变化 | P2 全程首次试玩 | 缩短重复段，增加视野变化；不增加无意义收集品 |
| 素材风格失配 | P1 同镜头对比 | 统一色板、粗糙度、轮廓和比例；关键车体最后定制 |
| 网页失焦、手柄断开或存储拒绝 | P1 起即覆盖 | 清输入、暂停、恢复时间基准；失败时明确降级 |

## 7. 整体验收方法

先做内部行为验证，再找 3–5 位未读策划的试玩者。人数只够发现明显问题，不用于声称统计显著。

固定观察：首次起步时间、第一次切低档原因、选择哪条分岔、受困多久、是否知道复位、是否主动停下看风景、是否完成环线。记录具体卡点和录屏，避免只问“好不好玩”。

体验门槛建议：多数试玩者能在 5 分钟内理解低档；无需口头指导完成三个景点；受困后知道至少一种恢复办法；至少两处风景引发主动停留。结合驾驶 D01–D12 与性能路线决定是否通过，不能用好看的截屏替代操作体验。

浏览器覆盖：目标桌面 Chrome、Edge，另在 Firefox 做完整通路；若列入 macOS，再单独验证 Safari 和手柄映射。手机触控暂不承诺。还需验证刷新读档、断网后的已加载会话、WebGL 不可用提示、关闭摄影后恢复驾驶。

## 8. 下一次开发任务的最小输入

只启动 P0：制作试验场、导入原型 SUV、完成固定步长驾驶和跟车相机，输出可运行网页、D01–D07 的结果与问题清单。不提前扩展地图或做运营界面。P0 的判断是“这辆车是否值得再开五分钟”。
