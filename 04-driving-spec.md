# 越野驾驶体验规格

以下全部是《Forest Trail》的拟定规格和验证方法，**不是原作物理参数，也不是已完成的测试记录**。目标是可感知、可控制的车辆重量和地形差异。

## 1. 先定义玩家应该感觉到什么

| 动作／情形 | 正确感觉 | 需要避免 |
| --- | --- | --- |
| 从静止起步 | 发动机负载增加，车身轻微后坐，再建立速度 | 一按键就瞬间获得固定速度 |
| 低速压过石块 | 对应轮先抬升，车身稍后跟随，出障碍后有受控回弹 | 整车沿地形法线硬贴过去，或轮胎穿地 |
| 上坡松油 | 车辆逐步失去速度，陡坡开始后退 | 无动力仍以原速上坡，或永远停在斜面 |
| 低档爬坡 | 小油门容易控制，最大车速低，牵引输出更充足 | 换档直接增加所有地面的摩擦，使车辆吸地 |
| 泥地踩深油门 | 轮胎／声音活跃但车辆推进有限，减油和换线有帮助 | 只把车速乘 0.5，毫无其它变化 |
| 横穿侧坡 | 车体侧倾，适当侧滑，仍有修正时间 | 小坡必翻，或永不侧翻 |
| 急刹停车 | 前悬压缩，车速和轮胎反馈一致 | 刹车直接把速度归零，或车身毫无反应 |
| 停下观景 | 镜头逐渐安定，自然声音显露 | 持续镜头晃动或怠速动画晃到看不清景物 |

## 2. 车辆模型和接触模型

先用一个动态刚体车身、四个轮地接触探测点和分离的四轮显示模型。重力、车身碰撞、悬挂支撑和轮胎作用都影响同一个车身刚体。车辆显示姿态跟随物理；不在渲染层另行覆盖车身位置来“贴地”。

建议从 Rapier 的射线车辆控制器做灰盒：官方接口提供逐轮引擎力、制动、转向、摩擦和悬挂控制，也可读取轮地接触信息。[T01 Rapier API](https://rapier.rs/javascript3d/classes/DynamicRayCastVehicleController.html) Three.js 有配套车辆示例，可用于核对接入和更新时序。[T02 官方示例](https://threejs.org/examples/physics_rapier_vehicle_controller.html)

**实现边界：**单射线轮胎不是完整圆形轮胎，薄石、直立台阶和边缘可能失去接触。灰盒先用带斜面的碰撞形状；若目标障碍仍不能稳定通过，再实现自己的多点探测／形状扫描轮胎。后者是额外工作，不是假定内置控制器已经支持。不要同时启用两套轮胎力而重复施力。

### 起始物理尺度

| 参数 | 调校起点 | 说明 |
| --- | --- | --- |
| 单位 | 米、秒、千克，Y 向上 | 模型导入先统一尺度 |
| 质量 | 1,500 kg | 仅作此车型起点 |
| 轴距／轮距 | 2.5 m / 1.65 m | 防止小模型套大车参数 |
| 轮胎半径 | 0.38 m | 四轮与地形尺寸一致 |
| 静态离地间隙 | 约 0.28 m | 在静态悬挂负载下测量 |
| 悬挂自由长度／工作总行程 | 约 0.42 m / 0.30 m | 自由长度、静态长度、API travel 的语义需分别校准 |
| 等效单轮弹簧刚度 | 25–35 kN/m | 对自写 SI 模型成立；不得照抄为 Rapier stiffness 数值 |
| 等效阻尼比 | 0.65–0.85 | 足够有起伏，但不连续弹跳 |
| 高档／低档目标最高速度 | 30 / 10 km/h | 用动力曲线平缓收束，而非硬截断速度 |
| 倒车目标最高速度 | 8 km/h | 便于退回重新选线 |
| 四轮合计峰值驱动力 | 高档约 5 kN，低档约 8 kN | 仍受接触载荷和抓地上限约束 |

在每轮约分担 375 kg 的静态近似下，30 kN/m 弹簧的静态压缩约 0.123 m。这只是核对车身不能凭空悬浮的尺度检查，不是对非平地载荷分配的承诺。重心高度先设得略低于外观车体中心，再通过侧坡验证；不要为了永不翻车把重心放到地面下。

若自写悬挂，使用有限的弹簧阻尼支撑：`F = clamp(k × compression − c × extensionVelocity, 0, Fmax)`。四轮力施加在实际接触／悬挂位置，以形成俯仰和侧倾。使用引擎控制器时遵循其参数定义，靠静态下沉、回弹和侧坡测试标定。

## 3. 输入与转向

- 键盘油门从 0 到 1 初始用约 0.35 秒，松开约 0.20 秒；手柄保留模拟量和小死区。
- 最大前轮转角从低速约 32° 平滑缩小到 30 km/h 时约 14°；转向本身有速率限制，不能瞬跳。
- 提供可选的“细腻油门”模式，限制输入增长速度；不偷偷改变最大爬坡能力。
- W/S 反向输入先制动，速度接近零后再进入另一方向；防止来回按键给车体施加不合理脉冲。
- 松开转向平缓回正。用户环视时不强行锁镜头，回正按钮明确有效。
- 手刹主要用于停车和低速控制，不把它调成必然漂移或瞬间翻车的按钮。

## 4. 地表差异：抓地、阻力与反馈一起变化

下表是相对干土的**设计倍率**，不是实测摩擦系数，也不是应直接写入物理引擎的 API 参数。用同车、同坡、同输入作相对验证。

| 地面／介质 | 可用牵引倍率 | 滚动／软土阻力倍率 | 反馈与行为 |
| --- | --- | --- | --- |
| 干土 | 1.00 | 1.0 | 主要基准，轻尘，操作宽容 |
| 碎石 | 0.80–0.90 | 1.2 | 制动略长、少量侧滑和石粒声音 |
| 干岩 | 0.95–1.05 | 0.8 | 有牵引但接触不均；悬挂和底盘几何更重要 |
| 湿泥 | 0.45–0.60 | 2.5–4.0 | 推进困难、空转明显，低速稳油和换线更有效 |
| 浅水 | 沿用水底材质 | 底材阻力加随水深增长的水阻 | 水花与水声；不能把所有水底统一算成泥 |

每个车轮独立查询地表，左右轮落在不同表面时行为不同；一块高度场的 collider ID 无法区分其内部泥区，需用轮胎世界接触位置查询材质掩码。材质过渡带约 0.5–1 m，参数平滑过渡，避免路面接缝突然甩车。

### 牵引的底线

概念上轮胎切向力受 `μ × 轮上法向载荷` 限制，横向与纵向共同消耗抓地。低档提高可控性和驱动力，但不会让低抓地路面无条件通过。不要用很大的侧向摩擦掩盖错误的重心或高速转向。

内置射线车未必提供足够真实的轮胎转速／滑移模型。首版可使用“请求牵引与实际推进的差异”作为视觉和音效代理，明确它不是工程级滑移率；真正影响车身的力仍来自一个一致的接触模型。无接触轮不应在空中持续给车身水平驱动力。

### 泥地与车辙的简化

泥坑本身是已制作的浅凹地形。附加软土阻力、湿亮表面、少量轮边泥点、短时车辙条带。首版不修改实时地形碰撞，不宣称车辙改变后车抓地；不得只把显示轮胎往地面里压而让物理接触保持悬空。

## 5. 涉水、爬坡与侧坡

主线浅水深度目标约 0.15–0.30 m，有明确出口。水深超过约 0.55 m 时给出危险提示，超过约 0.75 m 并持续 3 秒进入恢复流程；这些是游戏规则起点，不对应真实车辆涉水安全值。深水区用岸坡与颜色预告，避免正常玩家在出林瞬间意外失败。

上坡必须依靠牵引和驱动力，松油允许后退；制动停车应可靠。下坡可使用低档提供游戏化发动机制动，但不能完全消除重力或让车辆自动恒速。侧坡风险由坡度、速度、转向和重心共同产生，避免按“倾角超过 X”直接播放翻车动画。

## 6. 绞盘

状态：`收起 → 选择车辆连接点 → 选择锚点 → 已连接 → 牵引／停止 → 断开`。

选择范围初始 18 m，一次一根，前后连接点二选一。优先显示视野内、无遮挡的指定树干与岩桩；车速高于 2 km/h 不进入选点。玩家应能看到连接的是哪一点，不依赖随机最近树。

物理上只施加沿绳方向的拉力，作用于连接点；绳松弛不推车。收绳速率起点约 0.7 m/s，张力上限约 10 kN，施力平滑进入。车辆不能因拉索瞬间跃起；锚点位于车上方时需限制不合理抬升。首版不模拟绳子绕树和滑轮，连接被地形阻挡时拒绝并说明。

地图设计需确保泥地教学区前后均有有效锚点；这项内容约束比增加复杂自动选点算法更优先。即便没有锚点，仍可免费复位。

## 7. 摄像机

默认跟车距离起点约 7 m、高度约 3.2 m、垂直视野约 55°。看向车辆前方约 3 m，随速度稍增前视距离；低速镜头保留轮胎和近处路面。根据 16:9、16:10 和较宽屏幕验证遮挡与 HUD。

- 位置和朝向平滑跟随，阻尼不得造成转弯后仍看不到道路。
- 不完全跟随车身翻滚，保持稳定地平线；物理俯仰只少量传递到镜头。
- 相机到目标间作球形碰撞探测；靠近树干时拉近，离开后缓慢拉远，避免来回跳动。
- 必要时淡化遮挡车辆的树叶。树干遮挡优先用正确镜头位置解决。
- 环视后约 1.5 秒无输入才平滑回正，用户也可立即按回正键；绞盘与摄影模式暂停自动回正。
- 默认不添加速度抖动；悬挂冲击最多给轻微、可关闭的反馈。

## 8. 固定步长与稳定性

物理固定 60 Hz，渲染用插值。不要把每帧输入值当作已乘时间的力；验证 30、60、120 FPS 下的行驶一致性。帧过慢时限制追赶次数，切出页面暂停物理，返回清空时间积压和输入；不允许一次补几秒物理导致车辆弹飞。

车身高速碰撞需要连续碰撞检查或等效防穿透措施；小范围静态地形即可，不必给整个森林每片叶子创建碰撞体。刚体刷新顺序以选定库版本示例验证，不把所有控制器都想当然安排在物理更新后。

## 9. 驾驶试验场与验收

先制作一条约 100 m 试验路，包含以下独立小场景。测试时保留输入、速度、接地轮数、车身姿态、悬挂长度和复位原因；这些只在开发面板出现。

| 编号 | 场景与操作 | 初始通过标准 |
| --- | --- | --- |
| D01 | 平地起步、松油、刹停、倒车 | 无瞬间反向；停车后不持续漂移；切换输入不会抽搐 |
| D02 | 低档以约 4 km/h 通过 0.15 m 圆滑单轮凸起 | 对应轮响应，车身可见侧倾，离开后约 1.5 s 内主要振荡衰减 |
| D03 | 对角交错凸起／浅坑 | 左右轮悬挂不同步，车身不以完整地形法线瞬时对齐 |
| D04 | 10°、18°、25° 干燥坡道 | 18° 常规路可重复通过；25° 用于调挑战；松油后退、刹车可保持 |
| D05 | 同车同输入通过干土与泥地 | 泥段推进显著减弱，声画对应；不是永久卡死或完全无差别 |
| D06 | 左泥右土的分离地面 | 不发生跨边界瞬时甩车；有可控的牵引差异 |
| D07 | 12° 侧坡低速横行 | 玩家有修正余地，不无故翻车；更大倾角用来确认确实存在翻覆风险 |
| D08 | 浅溪进入／离开及深水恢复 | 阈值、提示、声音一致；复位不留在水里 |
| D09 | 泥坑接绞盘、牵引、松手、断开 | 无隔墙连接；车身不瞬移；能够回到可驾驶区域 |
| D10 | 主动翻车和卡底盘 | 1 秒长按后恢复至此前安全点，进度保留、速度清零 |
| D11 | 密林弯道、逆光入林、镜头回正 | 主要障碍可读，树叶不持续遮车，镜头不高频抽动 |
| D12 | 相同输入回放，渲染分别为 30/60/120 FPS | 20 秒后的行驶距离差异目标 ≤5%，不同帧率没有不同爬坡能力 |

数值阈值是开始调校用的验收目标，可在试玩后整体修订；不能看到一次通过就宣称模型准确。先调 D01–D04，再引入泥地和绞盘，最后回到完整森林检查。
